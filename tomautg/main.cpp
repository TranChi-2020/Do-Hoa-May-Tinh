#include <bits/stdc++.h>
#include <graphics.h>

using namespace std;

struct TOADO
{
	int x, y;
};

void FIll(TOADO A, TOADO B, TOADO C, int Color)
{
	int x, y, z;
}

int main()
{
	initwindow(800, 600);
	getch();
	closegraph();
	return 0;
}
